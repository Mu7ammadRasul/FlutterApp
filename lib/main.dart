import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

void main(List<String> args) {
  runApp(
    Material(
      color: Colors.blueAccent,
      child: Center(
      child: Text("hey this is my first application",
    textDirection: TextDirection.ltr,
        )
      )
    )
  );
}